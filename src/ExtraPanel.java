import javax.swing.*;
import java.awt.*;

public class ExtraPanel extends JPanel {
    public ExtraPanel() {
        setLayout(null);
        setBackground(Color.white);
        setVisible(true);
    }
}
